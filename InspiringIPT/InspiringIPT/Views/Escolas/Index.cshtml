@model IEnumerable<InspiringIPT.Models.Escola>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeEscola)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SiglaEscola)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeEscola)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SiglaEscola)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EscolaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EscolaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EscolaID })
        </td>
    </tr>
}

</table>
